import { Link } from 'react-router-dom';

interface Props {
    name?: string;
}

const Filtercards = ({   }: Props) => {
    return (
        <div>

        </div>
    );
};

export default Filtercards;
